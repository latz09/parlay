const SectionHeading = ({ title }) => {
	return (
		<div className='text-xl lg:text-3xl font-oswald uppercase mt-4 font-semibold opacity-80'>
			<span>{title} </span>
		</div>
	);
};

export default SectionHeading;
